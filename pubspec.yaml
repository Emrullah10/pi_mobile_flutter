name: mobile_flutter
description: "Whisper Pi Summary Hub - Mobile Control Center"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # State Management
  provider: ^6.1.0
  # HTTP
  http: ^1.2.0
  # Navigation
  go_router: ^14.0.0
  # UI
  google_fonts: ^6.2.0
  shimmer: ^3.0.0
  # Utility
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
